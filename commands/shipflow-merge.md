---
description: Merge a ShipFlow PR (confirms first)
argument-hint: <pr-number>
---

Merge PR #$1 with `renaiss-shipflow pr merge $1` — but ONLY after explicit user confirmation, since it triggers team-visible downstream workflows. Confirm before merging.
