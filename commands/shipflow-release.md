---
description: Cut a ShipFlow release (confirms first)
argument-hint: vX.Y.Z
---

Cut a release with `renaiss-shipflow release --tag $1 --json` — but ONLY after explicit user confirmation; releases trigger downstream workflows visible to the whole team. Confirm before releasing.
